# Node Headful

```bash
docker build --platform linux/amd64 -t <user>/nodeheadful .
```

Container

```bash
bash
bash init.sh
systemctl status xvfb
google-chrome --no-sandbox
systemctl status xvfb
```
